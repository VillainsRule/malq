import laravelCommons from './_constructor';

export default class tempmail44$com extends laravelCommons {
    domain = 'tempmail44.com';
    messageEndpoint = 'get_messages';
    customLaravelCookie = 'temp_mail_session';
    isFormData = false;
}