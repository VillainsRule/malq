import lwMessageCommons from './_constructor';

export default class txen$de extends lwMessageCommons {
    initialPath = '';
    refetchPath = '/mailbox';

    constructor() {
        super('txen.de', true);
    }
}