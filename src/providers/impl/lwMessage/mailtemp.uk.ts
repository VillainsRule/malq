import lwMessageCommons from './_constructor';

export default class mailtemp$uk extends lwMessageCommons {
    initialPath = '';
    refetchPath = '/mailbox';

    constructor() {
        super('mailtemp.uk', false);
    }
}