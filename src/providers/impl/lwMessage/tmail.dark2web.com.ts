import lwMessageCommons from './_constructor';

export default class tmail$dark2web$com extends lwMessageCommons {
    initialPath = '';
    refetchPath = '';

    constructor() {
        super('tmail.dark2web.com', true);
    }
}